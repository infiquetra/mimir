import 'package:flutter/material.dart';

class EncounterListTile extends StatelessWidget {
  const EncounterListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
