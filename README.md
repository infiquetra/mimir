# Mimir

This project includes a `formatBytes` utility function that converts byte counts to human-readable strings using binary units.

The implementation and tests were already present in the repository.